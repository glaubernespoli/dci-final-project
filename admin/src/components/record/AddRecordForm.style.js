import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  frm: {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '1.5rem',
    padding: '1rem',
    borderRadius: '.5rem'
  },

  btnContainer: {
    width: '100%'
  }
});
export default useStyles;
