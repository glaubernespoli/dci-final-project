import { makeStyles } from '@material-ui/core/styles';

const InfoStyle = makeStyles(() => ({
  root: {
    padding: '12vw'
  },
  imagePhone: {
    width: '100%',
    height: '100%'
  },
  imageIcon: {
    width: '100%',
    height: '80%'
  },
  imageIconContainer: {
    width: '50%'
  }
}));

export default InfoStyle;
